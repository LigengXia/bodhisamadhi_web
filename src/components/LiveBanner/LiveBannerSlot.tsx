// Docs/4 §3.21 · Docs/7 §3.2 — the sitewide live-banner slot. It sits directly
// above the nav and is **never activated in the MVP**: no live-session states
// exist yet, so it renders nothing and adds no height. Phase 16 fills it in.
export function LiveBannerSlot() {
  return null;
}
